# Puerto del servidor
PORT=3002

# Secreto para verificar tokens JWT (debe ser el mismo que auth-service)
JWT_SECRET=cambiar-este-secreto-en-produccion

# Ruta al archivo de base de datos SQLite
DB_PATH=./data/transactions.db
